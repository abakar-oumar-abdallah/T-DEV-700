version: '3.9'

services:
  backend:
    image: abakar98/jenkins-api:latest
    container_name: backend  
    env_file:
      - .env  # Charge les variables Supabase
    environment:
      - NODE_ENV=production
      - PORT=3000
    ports:
      - "3000:3000"
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    image: abakar98/jenkins-frontend:latest
    container_name: frontend
    ports:
      - "80:80"
    depends_on:
      - backend  # Attend que le backend soit démarré à voir
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge